---
title: "ツール"
description: "これは、さまざまな Vespa ユースケースのためのコマンドラインツールのリファレンスです。"
---

## vespa-analyze-onnx-model

ONNX モデルをロードして、メモリ使用量を分析し、入力タイプに基づいて出力タイプを推論/プローブします。

構文: `vespa-analyze-onnx-model <onnx-model> [options...]`

例(その他の例は [ONNX](/ja/ranking/onnx) を参照):

```bash
$ vespa-analyze-onnx-model Network.onnx
```

<table className="vespa-table">
  <thead>
    <tr>
      <th>オプション</th>
      <th>説明</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>&#42;&#42;--probe-types&#42;&#42;</td>
      <td>onnx モデルを使用して、入力タイプに基づいて出力タイプを推論/プローブします</td>
    </tr>
  </tbody>
</table>

## vespa-fbench

*vespa-fbench* は、Vespa システムの容量のベンチマークに使用します。使用方法と例については、[vespa-benchmarking](/ja/performance/benchmarking) を参照してください。

複数のホスト名とポートを使用して、負荷をラウンドロビンでクライアントに分散させることができます。

構文: `vespa-fbench [options] <hostname> <port>`

例:

```bash
$ vespa-fbench -n 10 -q query%03d.txt -s 300 -c 0 -o output%03d.txt -xy test.domain.com 8080
```

<table className="vespa-table">
  <thead>
    <tr>
      <th>オプション</th>
      <th>説明</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>&#42;&#42;-H &#42;header&#42;&#42;&#42;</td>
      <td>各 GET リクエストに追加のヘッダーを付加します。</td>
    </tr>
    <tr>
      <td>&#42;&#42;-A &#42;assign authority&#42;&#42;&#42;</td>
      <td>hostname:port。送信される Host: ヘッダーをオーバーライドします。</td>
    </tr>
    <tr>
      <td>&#42;&#42;-a &#42;str&#42;&#42;&#42;</td>
      <td>各クエリーに文字列を付加します</td>
    </tr>
    <tr>
      <td>&#42;&#42;-n &#42;numClients&#42;&#42;&#42;</td>
      <td>vespa-fbench を &#42;numClients&#42; 個のクライアントで並列実行します。指定されない場合、vespa-fbench はデフォルト値の &#42;10&#42; クライアントを使用します。</td>
    </tr>
    <tr>
      <td>&#42;&#42;-c &#42;cycleTime&#42;&#42;&#42;</td>
      <td>各クライアントは <code>{`<num>`}</code> ミリ秒ごとにリクエストを発行します [1000]('-1' -&gt; サイクル時間はレスポンス時間の 2 倍になります)</td>
    </tr>
    <tr>
      <td>&#42;&#42;-l &#42;limit&#42;&#42;&#42;</td>
      <td>成功したリクエストとみなす最小レスポンスサイズ [0]</td>
    </tr>
    <tr>
      <td>&#42;&#42;-i &#42;ignoreCount&#42;&#42;&#42;</td>
      <td>最初の <code>{`<num>`}</code> 件の結果をログに記録しません。-1 はログを記録しないことを意味します [0]</td>
    </tr>
    <tr>
      <td>&#42;&#42;-s &#42;seconds&#42;&#42;&#42;</td>
      <td>テストを <code>{`<num>`}</code> 秒間実行します。-1 は無期限を意味します [60]</td>
    </tr>
    <tr>
      <td>&#42;&#42;-q &#42;queryFilePattern&#42;&#42;&#42;</td>
      <td>入力クエリーファイルを定義するパターン。例: &#42;query%03d.txt&#42;(このパターンは sprintf でファイル名の生成に使用されます)。POST を使用する場合を除き、クエリーファイルは 1 行につき 1 つのクエリーを持ち、各行は <code>{`/search/`}</code> で始まります:<br/><br/><code>{`/search/?yql=select%20%2A%20from%20sources%20%2A%20where%20true`}</code></td>
    </tr>
    <tr>
      <td>&#42;&#42;-P&#42;&#42;</td>
      <td>リクエストに GET の代わりに POST を使用します。1 クエリーにつき 2 行で、形式は次のとおりです:<br/><br/><code>{`/search/`}</code><br/><code>{`{"yql" : "select * from sources * where true"}`}</code> <br/><br/>「/」で始まる行は URL パスとして扱われ、それに続く行はコンテンツとして扱われます(これらの行は「/」で始めることはできません)。デフォルトのコンテンツタイプは &#42;"Content-Type: application/json"&#42; です。&#42;-H&#42; を参照してください。</td>
    </tr>
    <tr>
      <td>&#42;&#42;-o &#42;outputFilePattern&#42;&#42;&#42;</td>
      <td>指定したパターンの出力ファイルにクエリー結果を保存します(デフォルトは保存しません)。</td>
    </tr>
    <tr>
      <td>&#42;&#42;-r &#42;restartLimit&#42;&#42;&#42;</td>
      <td>各クエリーファイルを再利用する回数。-1 は無制限を意味します [-1]</td>
    </tr>
    <tr>
      <td>&#42;&#42;-m &#42;maxLineSize&#42;&#42;&#42;</td>
      <td>入力クエリーファイルの最大行サイズ [8192]。最小値 [1024] より小さくすることはできません。</td>
    </tr>
    <tr>
      <td>&#42;&#42;-p &#42;seconds&#42;&#42;&#42;</td>
      <td><code>{`<num>`}</code> 秒ごとにサマリーを出力します。test ブランチから vespa-fbench をインストールした場合にのみ利用できます。</td>
    </tr>
    <tr>
      <td>&#42;&#42;-k&#42;&#42;</td>
      <td>HTTP キープアライブを有効にします。</td>
    </tr>
    <tr>
      <td>&#42;&#42;-d&#42;&#42;</td>
      <td>POST リクエストのコンテンツを Base64 デコードします</td>
    </tr>
    <tr>
      <td>&#42;&#42;-x&#42;&#42;</td>
      <td>ベンチマークデータのレポートを出力ファイルに書き込みます: <br/><br/> &#42;&#42;NumHits&#42;&#42;<br/> 返されたヒット数 <br/><br/> &#42;&#42;NumFastHits&#42;&#42; <br/> 返された実際のドキュメントヒット数 <br/> <br/>&#42;&#42;TotalHitCount&#42;&#42; <br/> クエリーの合計ヒット数 <br/> <br/> &#42;&#42;QueryHits&#42;&#42; <br/>クエリーで指定されたヒット数 <br/> <br/> &#42;&#42;QueryOffset&#42;&#42;<br/> クエリーで指定されたオフセット <br/><br/> &#42;&#42;NumErrors&#42;&#42; <br/> 返されたエラーヒット数 <br/> <br/> &#42;&#42;NumGroupHits&#42;&#42; <br/> 返されたグルーピングヒット数 <br/><br/>  &#42;&#42;SearchTime&#42;&#42; <br/> 検索に使用された時間。1 フェーズ検索の場合はクエリー全体の時間、2 フェーズ検索の場合は第 1 フェーズの時間 <br/> <br/> &#42;&#42;AttributeFetchTime&#42;&#42; <br/> アトリビュートの取得に使用された時間。1 フェーズ検索の場合は 0 <br/> <br/> &#42;&#42;FillTime&#42;&#42; <br/> サマリーの取得に使用された時間。1 フェーズ検索の場合は 0</td>
    </tr>
    <tr>
      <td>&#42;&#42;-y&#42;&#42;</td>
      <td>カバレッジに関するデータを出力ファイルに書き込みます(-x と併用する必要があります)。<br/><br/> &#42;&#42;DocsSearched&#42;&#42; <br/> 検索されたノード内のドキュメントの総数  <br/><br/> &#42;&#42;NodesSearched&#42;&#42; <br/> 使用された検索ノードの総数 <br/> <br/>&#42;&#42;FullCoverage&#42;&#42;  <br/>true の場合は 1、false の場合は 0</td>
    </tr>
    <tr>
      <td>&#42;&#42;-z&#42;&#42;</td>
      <td>クライアント間で分配される単一のクエリーファイルを使用します。</td>
    </tr>
    <tr>
      <td>&#42;&#42;-C &#42;file&#42;&#42;&#42;</td>
      <td>クライアント証明書のファイル名</td>
    </tr>
    <tr>
      <td>&#42;&#42;-K &#42;file&#42;&#42;&#42;</td>
      <td>クライアント秘密鍵のファイル名</td>
    </tr>
    <tr>
      <td>&#42;&#42;-D&#42;&#42;</td>
      <td>T/C/K が使用されていない場合、環境の TLS 設定を使用します</td>
    </tr>
  </tbody>
</table>

デフォルトの出力:

<table className="vespa-table">
  <thead>
    <tr>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>&#42;&#42;connection reuse count&#42;&#42;</td>
      <td>別のリクエストを発行するために HTTP 接続が再利用された回数を示します。この数値は、-k スイッチ(HTTP キープアライブの有効化)を使用した場合にのみ表示されることに注意してください。</td>
    </tr>
    <tr>
      <td>&#42;&#42;clients&#42;&#42;</td>
      <td>-n パラメーターのエコー。</td>
    </tr>
    <tr>
      <td>&#42;&#42;cycle time&#42;&#42;</td>
      <td>-c パラメーターのエコー。</td>
    </tr>
    <tr>
      <td>&#42;&#42;lower response limit&#42;&#42;</td>
      <td>-l パラメーターのエコー。</td>
    </tr>
    <tr>
      <td>&#42;&#42;skipped requests&#42;&#42;</td>
      <td>vespa-fbench によってスキップされたリクエストの数。vespa-fbench は通常、クエリー URL を含む行が事前定義された制限を超えた場合にリクエストをスキップします。スキップされたリクエストが統計結果に与える影響はごくわずかです。</td>
    </tr>
    <tr>
      <td>&#42;&#42;failed requests&#42;&#42;</td>
      <td>失敗したリクエストの数。結果の読み取り中にエラーが発生した場合、または結果に含まれるバイト数が「lower response limit」より少なかった場合、リクエストは失敗としてマークされます。</td>
    </tr>
    <tr>
      <td>&#42;&#42;successful requests&#42;&#42;</td>
      <td>成功したリクエストの数。実行された各リクエストは、成功または失敗のいずれかとしてカウントされます。スキップされたリクエスト(上記参照)は実行されないため、カウントされません。</td>
    </tr>
    <tr>
      <td>&#42;&#42;cycles not held&#42;&#42;</td>
      <td>維持できなかったサイクルの数。サイクル時間は -c パラメーターで指定します。これは、クライアントが新しいリクエストを実行する頻度を定義します。ただし、クライアントは前のリクエストの結果が得られる前に別のリクエストを実行することはできません。前のリクエストが完了していないためにクライアントが「時間どおりに」新しいリクエストを開始できなかった場合、この値が増加します。</td>
    </tr>
    <tr>
      <td>&#42;&#42;minimum response time&#42;&#42;</td>
      <td>最小レスポンス時間。レスポンス時間は、リクエストがサーバーに送信される直前から、サーバーから結果が得られるまでの期間として測定されます。</td>
    </tr>
    <tr>
      <td>&#42;&#42;maximum response time&#42;&#42;</td>
      <td>最大レスポンス時間。レスポンス時間は、リクエストがサーバーに送信される直前から、サーバーから結果が得られるまでの期間として測定されます。</td>
    </tr>
    <tr>
      <td>&#42;&#42;average response time&#42;&#42;</td>
      <td>平均レスポンス時間。レスポンス時間は、リクエストがサーバーに送信される直前から、サーバーから結果が得られるまでの期間として測定されます。</td>
    </tr>
    <tr>
      <td>&#42;&#42;X percentile&#42;&#42;</td>
      <td>レスポンス時間のサンプルの X パーセンタイル。レスポンス時間のサンプルの X パーセントがこの値を下回るように選択された値です。パーセンタイルを計算するために、実行時にクライアントごとにレスポンス時間のヒストグラムが維持され、テスト実行の終了後にマージされます。パーセンタイル値がこのヒストグラムの上限を超える場合、その値は近似され(そのため精度が低くなり)、「(approx)」というマークが付きます。</td>
    </tr>
    <tr>
      <td>&#42;&#42;actual query rate&#42;&#42;</td>
      <td>1 秒あたりの平均クエリー数(QPS)。</td>
    </tr>
    <tr>
      <td>&#42;&#42;utilization&#42;&#42;</td>
      <td>サーバーが(成功した)リクエストを完了するのを待つために使用された時間の割合。リクエストが失敗した場合、クライアントは失敗したリクエストに費やした時間を「無駄に」したことになるため、utilization は低下することに注意してください。</td>
    </tr>
    <tr>
      <td>&#42;&#42;zero hit queries&#42;&#42;</td>
      <td>Vespa でヒットが 0 件だったクエリーの数</td>
    </tr>
  </tbody>
</table>

## vespa-makefsa

*vespa-makefsa* は、フレーズのリストを *有限状態オートマトン*(FSA)ファイルにコンパイルするために使用します。FSA ファイルは、[クエリーのフレーズ化と書き換え](/ja/linguistics/query-rewriting)で使用されます。

入力ファイルが指定されていない場合は、標準入力が使用されます。

構文: `vespa-makefsa [-h] [-b] [-B] [-e] [-n] [-s bytes] [-z bytes] [-t] [-p] [-i] [-S serial] [-v] [-V] [input_file] output_file`

<table className="vespa-table">
  <thead>
    <tr>
      <th>オプション</th>
      <th>説明</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>&#42;&#42;-h&#42;&#42;</td>
      <td>ヘルプテキスト</td>
    </tr>
    <tr>
      <td>&#42;&#42;-b&#42;&#42;</td>
      <td>Base64 エンコードされた情報付きのバイナリ入力形式を使用します</td>
    </tr>
    <tr>
      <td>&#42;&#42;-B&#42;&#42;</td>
      <td>raw 形式のバイナリ入力形式を使用します</td>
    </tr>
    <tr>
      <td>&#42;&#42;-e&#42;&#42;</td>
      <td>情報なしのテキスト入力形式を使用します(デフォルト)</td>
    </tr>
    <tr>
      <td>&#42;&#42;-s bytes&#42;&#42;</td>
      <td>数値情報のデータサイズ: 1、2、または 4(デフォルト)</td>
    </tr>
    <tr>
      <td>&#42;&#42;-z bytes&#42;&#42;</td>
      <td>バイナリ情報(-B)のデータサイズ(0 は NUL 終端を意味します)</td>
    </tr>
    <tr>
      <td>&#42;&#42;-t&#42;&#42;</td>
      <td>テキスト入力形式を使用します</td>
    </tr>
    <tr>
      <td>&#42;&#42;-p&#42;&#42;</td>
      <td>完全ハッシュ(perfect hash)付きのオートマトンを構築します</td>
    </tr>
    <tr>
      <td>&#42;&#42;-i&#42;&#42;</td>
      <td>入力形式にかかわらず、情報文字列を無視します</td>
    </tr>
    <tr>
      <td>&#42;&#42;-S serial&#42;&#42;</td>
      <td>シリアル番号</td>
    </tr>
    <tr>
      <td>&#42;&#42;-v&#42;&#42;</td>
      <td>詳細出力</td>
    </tr>
    <tr>
      <td>&#42;&#42;-V&#42;&#42;</td>
      <td>バージョンを表示します</td>
    </tr>
  </tbody>
</table>

## vespa-query-profile-dump-tool

一連のディメンション値に対して、解決済みのすべてのクエリープロファイルプロパティをダンプします

構文:`vespa-query-profile-dump-tool dump [query-profile] [dir]? [parameters]?`

例:

```bash
dump default                           # dumps the 'default' profile non-variant values in the current dir

dump default x=x1&y=y1                 # dumps the 'default' profile resolved with dimensions values x=x1 and y=y1 in the current dir

dump default myapppackage              # dumps the 'default' profile non-variant values in myapppackage/search/query-profiles

dump default dev/myprofiles x=x1&y=y1  # dumps the 'default' profile resolved with dimensions values x=x1 and y=y1 in dev/myprofiles
```

<table className="vespa-table">
  <thead>
    <tr>
      <th>オプション</th>
      <th>説明</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>&#42;&#42;query-profile&#42;&#42;</td>
      <td>値をダンプするクエリープロファイルの名前</td>
    </tr>
    <tr>
      <td>&#42;&#42;dir&#42;&#42;</td>
      <td>アプリケーション・パッケージまたはクエリープロファイルディレクトリへのパス。デフォルト: カレントディレクトリ</td>
    </tr>
    <tr>
      <td>&#42;&#42;parameters&#42;&#42;</td>
      <td>解決時に使用される、HTTP リクエスト形式でエンコードされたディメンションキー。デフォルト: なし</td>
    </tr>
  </tbody>
</table>
