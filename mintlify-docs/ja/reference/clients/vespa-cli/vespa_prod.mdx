---
title: "vespa prod"
description: "アプリケーション・パッケージを Vespa Cloud のプロダクションにデプロイします"
---

### Synopsis

アプリケーション・パッケージを Vespa Cloud のプロダクションにデプロイします。

アプリケーション・パッケージを設定し、Vespa Cloud のプロダクションにデプロイします。

```bash
vespa prod [flags]
```

### 例

```bash
$ vespa prod init
$ vespa prod deploy
```

### オプション

```text
  -h, --help   help for prod
```

### 親コマンドから継承されるオプション

```text
  -a, --application string   The application to use. Format "tenant.application.instance" - instance is optional (tenant required for cloud targets)
  -C, --cluster string       The container cluster to use. This is only required for applications with multiple clusters
  -c, --color string         Whether to use colors in output. Must be "auto", "never", or "always" (default "auto")
  -i, --instance string      The instance of the application to use
  -q, --quiet                Print only errors
  -t, --target string        The target platform to use. Must be "local", "cloud", "hosted" or an URL (default "local")
  -z, --zone string          The zone to use. This defaults to a dev zone (cloud only)
```

### 関連項目

* [vespa](/ja/reference/clients/vespa-cli/vespa)	 - Vespa.ai のコマンドラインツール
* [vespa prod deploy](/ja/reference/clients/vespa-cli/vespa_prod_deploy)	 - アプリケーションをプロダクションにデプロイする
* [vespa prod init](/ja/reference/clients/vespa-cli/vespa_prod_init)	 - プロダクションのデプロイメント用に service.xml と deployment.xml を変更する

