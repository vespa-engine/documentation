---
title: "vespa status"
description: "Vespa のエンドポイントとステータスを表示します"
---

### Synopsis

Vespa のエンドポイントとステータスを表示します。

このコマンドは、デプロイされた Vespa アプリケーションの現在のエンドポイントとそのステータスを表示します。

```bash
vespa status [flags]
```

### 例

```bash
$ vespa status
$ vespa status --cluster mycluster
$ vespa status --cluster mycluster --wait 600
$ vespa status --format plain --cluster mycluster
$ vespa status --no-verify
```

### オプション

```bash
      --format string   Output format. Must be 'human' (human-readable), 'plain' (cluster URL only), or 'json' (default "human")
  -h, --help            help for status
      --no-verify       Skip checking service status (control plane only)
  -w, --wait int        Number of seconds to wait for service(s) to become ready. 0 to disable (default 0)
```

### 親コマンドから継承されるオプション

```bash
  -a, --application string   The application to use. Format "tenant.application.instance" - instance is optional (tenant required for cloud targets)
  -C, --cluster string       The container cluster to use. This is only required for applications with multiple clusters
  -c, --color string         Whether to use colors in output. Must be "auto", "never", or "always" (default "auto")
  -i, --instance string      The instance of the application to use
  -q, --quiet                Print only errors
  -t, --target string        The target platform to use. Must be "local", "cloud", "hosted" or an URL (default "local")
  -z, --zone string          The zone to use. This defaults to a dev zone (cloud only)
```

### 関連項目

* [vespa](/ja/reference/clients/vespa-cli/vespa)	 - Vespa.ai のコマンドラインツール
* [vespa status deploy](/ja/reference/clients/vespa-cli/vespa_status_deploy)	 - Vespa デプロイサービスのステータスを表示する
* [vespa status deployment](/ja/reference/clients/vespa-cli/vespa_status_deployment)	 - Vespa デプロイメントのステータスを表示する
* [vespa status endpoint](/ja/reference/clients/vespa-cli/vespa_status_endpoint)	 - ステータスを確認せずに Vespa のエンドポイントを表示する

