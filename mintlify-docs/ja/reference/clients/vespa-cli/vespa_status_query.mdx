---
title: "vespa status query"
description: "クエリーサービスが利用可能な状態であることを確認します (デフォルト)"
---

```bash
vespa status query [flags]
```

### 例

```bash
$ vespa status query
```

### オプション

```bash
  -h, --help       help for query
  -w, --wait int   Number of seconds to wait for a service to become ready. 0 to disable (default 0)
```

### 親コマンドから継承されるオプション

```bash
  -a, --application string   The application to use
  -C, --cluster string       The container cluster to use. This is only required for applications with multiple clusters
  -c, --color string         Whether to use colors in output. Must be "auto", "never", or "always" (default "auto")
  -i, --instance string      The instance of the application to use
  -q, --quiet                Print only errors
  -t, --target string        The target platform to use. Must be "local", "cloud", "hosted" or an URL (default "local")
  -z, --zone string          The zone to use. This defaults to a dev zone
```

### 関連項目

* [vespa status](/ja/reference/clients/vespa-cli/vespa_status)	 - サービスが利用可能な状態であることを確認する (デフォルトではクエリー)

