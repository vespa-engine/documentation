---
title: "vespa completion"
description: "指定したシェル用の自動補完スクリプトを生成します"
---

### Synopsis

指定したシェル用の vespa の自動補完スクリプトを生成します。生成されたスクリプトの使用方法の詳細は、各サブコマンドのヘルプを参照してください。


### オプション

```bash
  -h, --help   help for completion
```

### 親コマンドから継承されるオプション

```bash
  -a, --application string   The application to use (cloud only)
  -C, --cluster string       The container cluster to use. This is only required for applications with multiple clusters
  -c, --color string         Whether to use colors in output. Must be "auto", "never", or "always" (default "auto")
  -i, --instance string      The instance of the application to use (cloud only)
  -q, --quiet                Print only errors
  -t, --target string        The target platform to use. Must be "local", "cloud", "hosted" or an URL (default "local")
  -z, --zone string          The zone to use. This defaults to a dev zone (cloud only)
```

### 関連項目

* [vespa](/ja/reference/clients/vespa-cli/vespa)	 - Vespa.ai のコマンドラインツール
* [vespa completion bash](/ja/reference/clients/vespa-cli/vespa_completion_bash)	 - bash 用の自動補完スクリプトを生成する
* [vespa completion fish](/ja/reference/clients/vespa-cli/vespa_completion_fish)	 - fish 用の自動補完スクリプトを生成する
* [vespa completion powershell](/ja/reference/clients/vespa-cli/vespa_completion_powershell)	 - powershell 用の自動補完スクリプトを生成する
* [vespa completion zsh](/ja/reference/clients/vespa-cli/vespa_completion_zsh)	 - zsh 用の自動補完スクリプトを生成する

