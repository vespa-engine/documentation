---
title: "vespa document"
description: "Vespa に対して単一のドキュメント操作を発行します"
---

### 概要

Vespa に対して単一のドキュメント操作を発行します。

操作は [/ja/reference/schemas/document-json-format#document-operations](/ja/reference/schemas/document-json-format#document-operations) に記載されている形式でなければなりません

このコマンドが正常に完了すると、そのドキュメントは以降のあらゆる get またはクエリー操作で参照可能であることが保証されます。

高いスループットでフィードするには、これの代わりに [/ja/reference/clients/vespa-cli/vespa_feed.html](/ja/reference/clients/vespa-cli/vespa_feed) を使用すべきです。

```bash
vespa document json-file [flags]
```

### 例

```bash
$ vespa document src/test/resources/A-Head-Full-of-Dreams.json
```

### オプション

```bash
  -d, --data string      Document data to use instead of reading from file or stdin
      --header strings   Add a header to the HTTP request, on the format 'Header: Value'. This can be specified multiple times
  -h, --help             help for document
  -T, --timeout int      Timeout for the document request in seconds (default 60)
  -v, --verbose          Print the equivalent curl command for the document operation
  -w, --wait int         Number of seconds to wait for service(s) to become ready. 0 to disable (default 0)
```

### 親コマンドから継承されるオプション

```bash
  -a, --application string   The application to use. Format "tenant.application.instance" - instance is optional (tenant required for cloud targets)
  -C, --cluster string       The container cluster to use. This is only required for applications with multiple clusters
  -c, --color string         Whether to use colors in output. Must be "auto", "never", or "always" (default "auto")
  -i, --instance string      The instance of the application to use
  -q, --quiet                Print only errors
  -t, --target string        The target platform to use. Must be "local", "cloud", "hosted" or an URL (default "local")
  -z, --zone string          The zone to use. This defaults to a dev zone (cloud only)
```

### 関連項目

* [vespa](/ja/reference/clients/vespa-cli/vespa)	 - Vespa.ai のコマンドライン・ツール
* [vespa document get](/ja/reference/clients/vespa-cli/vespa_document_get)	 - 1 つ以上のドキュメントを取得します
* [vespa document put](/ja/reference/clients/vespa-cli/vespa_document_put)	 - ドキュメントを Vespa に書き込みます
* [vespa document remove](/ja/reference/clients/vespa-cli/vespa_document_remove)	 - ドキュメントを Vespa から削除します
* [vespa document update](/ja/reference/clients/vespa-cli/vespa_document_update)	 - 既存のドキュメントの一部のフィールドを変更します
