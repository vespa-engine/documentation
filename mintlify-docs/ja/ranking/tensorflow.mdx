---
# Copyright Vespa.ai. All rights reserved.
title: "TensorFlow モデルによるランキング"
sidebarTitle: "TensorFlow モデルの使用"
---

Vespa は、ONNX フォーマットに変換された TensorFlow モデルをインポートできます。チュートリアル [TensorFlow: Deploy model to Vespa through ONNX](https://vespa-engine.github.io/learntorank-DEPRECATED/notebooks/tensorflow-via-onnx.html) では、Learning-to-Rank (LTR) モデルの学習から Vespa へのデプロイまでのエンドツーエンドの例を紹介しています。このチュートリアルは [Jupyter Notebook](https://github.com/vespa-engine/learntorank-DEPRECATED/blob/main/notebooks/tensorflow-via-onnx.ipynb) を実行することで再現できます。

上記チュートリアルで扱う主なステップは次のとおりです。

- `tf_model` を定義して学習する。
- モデルをディスクに保存する: `tf_model.save("tf_model_file")`
- `tf2onnx` ライブラリーでモデルを ONNX に変換する:

```sh
$ python3 -m tf2onnx.convert --saved-model tf_model_file --output tf_model.onnx
```

- `onnx` ライブラリーで想定される入出力フォーマットを確認する:

```python
import onnx
m = onnx.load("simpler_keras_model.onnx")
m.graph.input # check input format
m.graph.output # check output format
```

- Vespa の .sd ファイルにモデルを含める

```js expandable
schema msmarco {
        document msmarco {
            field id type string {
                indexing: summary | attribute
            }
            field text type string {
                indexing: summary | index
            }
        }
        onnx-model ltr_tensorflow {
            file: files/tf_model.onnx
            input input: vespa_input
            output dense: dense
        }
        rank-profile tensorflow {
            function vespa_input() {
                expression {
                    tensor<float>(x[1],y[3]):[
                    	[fieldMatch(text).queryCompleteness, 
                    	fieldMatch(text).significance, 
                    	nativeRank(text)]
                    ]
                }
            }
            first-phase {
                expression: sum(onnx(ltr_tensorflow).dense)
            }
            summary-features {
                onnx(ltr_tensorflow)
                fieldMatch(text).queryCompleteness
                fieldMatch(text).significance
                nativeRank(text)
            }
        }
    }
```

